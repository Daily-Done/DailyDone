[build]
builder = { type = "nixpacks", plan = { providers = ["java"] } }

[deploy]
preDeployCommand = "cd backend && mvn clean package -DskipTests"
startCommand = "cd backend && java -jar target/*.jar"